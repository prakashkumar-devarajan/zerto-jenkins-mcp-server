services:
  jenkins-mcp-server:
    build: .
    container_name: jenkins-mcp-server-stdio
    environment:
      - JENKINS_URL=https://zbuildsrv1.zerto.local:8443/
      - JENKINS_USER=${JENKINS_USER}
      - JENKINS_TOKEN=${JENKINS_TOKEN}
      - NODE_EXTRA_CA_CERTS=/app/Zerto-Root-CA.crt
    volumes:
      - ./Zerto-Root-CA.crt:/app/Zerto-Root-CA.crt:ro
    # Keep container running without starting server
    command: tail -f /dev/null
    restart: unless-stopped
